logging:
  level:
    com.github.kerner1000: debug

server:
  port: 8080

feign:
  client:
    config:
      default:
        loggerLevel: NONE

app.terra.sleep-between-calls: 800

---
spring:
  config:
    activate:
      on-profile: docker

server.port: 8080

logging:
  level:
    root: INFO
    com.github.kerner1000: INFO

