logging:
  level:
    com.github.kerner1000: debug

server:
  port: 8080

feign:
  client:
    config:
      default:
        loggerLevel: NONE

app.terra.sleep-between-calls: 800
app.terra.write-transactions: true

spring:
  main:
    banner-mode: off

